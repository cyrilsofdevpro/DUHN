DUHN Technologies — Static portfolio website scaffold

How to view locally:

1. Open the `duhn-technologies` folder in your editor.
2. Open `index.html` in the browser (recommended: use Live Server extension).

Stack: HTML5, CSS3, Vanilla JS

Note: Replace placeholder images in `assets/images` and update contact form endpoint in `contact.html`.